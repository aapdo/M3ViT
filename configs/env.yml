# Deprecated: use configs/path_env.yml instead.
root_dir: ${HOME}/m3vit

db_root_dir: ${HOME}/multi_task_datasets
dataset_roots:
  ImageNet1K: ${HOME}/multi_task_datasets/imagenet
  PASCAL_MT: ${HOME}/multi_task_datasets/PASCAL_MT
  NYUD_MT: ${HOME}/multi_task_datasets/NYUD_MT
  cityscapes: ${HOME}/multi_task_datasets/cityscapes

seism_root: /path/to/seism/

# Optional: Hugging Face access token (e.g., for gated datasets like ILSVRC/imagenet-1k)
# Prefer environment variable usage: export HF_TOKEN=...
huggingface_access_token: ${HF_TOKEN}
